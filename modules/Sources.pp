// This is a group directory: a directory level above a number of
// source subdirectories.

#define DIR_TYPE group

// The modules directory always depends on the metalibs directory.
#define DEPENDS metalibs
